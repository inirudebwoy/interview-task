[tox]
envlist = py{27,33,34}

[pytest]
addopts = --ignore=setup.py
python_files = *.py
python_functions = test_
norecursedirs = .tox .git

[testenv]
deps =
     pytest
     mock
     coverage
     pytest-cov
commands =
     py.test --cov=babynames

[flake8]
max-line-length = 100
